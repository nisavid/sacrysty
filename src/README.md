# Core implementation

This directory is reserved for one primary core distribution after the
repository-native implementation map settles its substrate and package or
library coordinates.

The implementation language, package coordinates, module layout, public
facade, command grammar, schemas, authority semantics, custody model, and
protocol implementation are intentionally unset. No source code exists yet.
