# Signing fixtures

`message.bin` is the canonical disposable byte fixture. Keys and signatures are
generated ephemerally by `conformance/run-signing-profile.sh`; no key material,
identity, host binding, or provider payload is committed.
