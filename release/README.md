# Release metadata

This directory is reserved for versioned public release metadata after the
owning design work settles its format and authentication requirements.

The repository currently has no release-metadata instance, tag, package,
artifact, or release. This directory does not define release authority,
protocol semantics, or an operational ceremony.
