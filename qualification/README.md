# Qualification

This directory is reserved for public, value-free qualification formats,
records, and prospective status records after their owning contracts settle the
required evidence.

A record must bind its claim to immutable inputs and state its limitations and
invalidation triggers. No implementation, adapter, profile, protocol, or
platform is currently qualified.
